# To create an environment with this configuration, use the following command:
# conda env create -f env.yml

# After the environment is created, activate it and manuall install torch,
# torchaudio, and torchsummary.

name: drum-tracker
channels:
  - conda-forge
  - defaults
dependencies:
  - librosa
  - matplotlib
  - numpy
  - pandas
  - pip
  - python
  - seaborn
  - tqdm
  - pip:
    - docopt
    - midiutil